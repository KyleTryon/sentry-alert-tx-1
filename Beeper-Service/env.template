# Server Configuration
NODE_ENV=development
PORT=3000
HOST=0.0.0.0

# Domain for Caddy (used in Caddyfile as {$DOMAIN})
# Example: shoutify.org
DOMAIN=

# MQTT Configuration
# IMPORTANT:
# - When running via docker-compose, the broker is the 'mosquitto' service.
#   Use the service name on the internal network:
#   MQTT_BROKER_URL=mqtt://mosquitto:1883
# - Do NOT use localhost inside the beeper-service container for the broker.
# - External devices (ESP32) should connect to the docker host IP:1883.
MQTT_BROKER_URL=mqtt://mosquitto:1883
MQTT_USERNAME=beeper-service
MQTT_PASSWORD=your-mqtt-password-here
MQTT_CLIENT_ID=beeper-service
MQTT_TOPIC_PREFIX=alerts
MQTT_QOS=1

# Sentry Configuration
SENTRY_WEBHOOK_SECRET=your-webhook-secret-here
SENTRY_ALLOWED_IPS=

# Logging Configuration
LOG_LEVEL=info

# Health Check Configuration
HEALTH_CHECK_INTERVAL_MS=30000
