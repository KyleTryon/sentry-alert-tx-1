# MQTT Access Control List
# Format: user <username>
#         topic [read|write|readwrite] <topic>

# Admin user - full access
user admin
topic readwrite #

# Beeper Service - can publish to alerts topics only
user beeper-service
topic write alerts/+/+
topic read $SYS/broker/uptime
topic read $SYS/broker/clients/connected

# AlertTX Device - can subscribe to alerts topics only
user alerttx-device
topic read alerts/+/+
topic write device/alerttx/status
topic write device/alerttx/heartbeat

# Default topic permissions
# Deny all for unauthenticated users
pattern read $SYS/broker/uptime
pattern read $SYS/broker/version
